import { FormErrorsMessagePipe } from './form-errors-message.pipe-pipe';

describe('FormErrorsMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormErrorsMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
